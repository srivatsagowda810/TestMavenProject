pipeline {
	agent any 
	stages {
	  stage('build') {
		steps {
		  echo 'build babayaga'
		  withMaven(maven: 'maven3') {
    sh 'mvn clean install'
}
		}
	  }

	  stage('test') {
		steps {
		  echo 'babayaga test'
		}
	  }

	  stage('release') {
		steps {
		  echo 'relaese'
		}
	  }

}

}
